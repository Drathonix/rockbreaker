modLoader="javafml"
loaderVersion="${loader_ver_range}"
license="${license}"
issueTrackerURL="${issue_tracker}"

[[mods]]
modId="${id}"
version="${version}"
displayName="${name}"
logoFile="${icon}"
authors="${authors}"
description='''
${description}
'''

${mixin_field}

${dependencies_field}
